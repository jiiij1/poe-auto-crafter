
```markdown
# POE 剪贴板匹配与自定义动作工具 使用说明文档

## 一、软件简介

本软件用于读取游戏内复制的装备文本，根据预设的蓝色词条、红色词条进行匹配，并在匹配失败时自动执行自定义动作，例如“改造”“重铸点金”等。

主要功能：

- 读取游戏内复制的装备剪贴板文本；
- 根据蓝色词条、红色词条判断是否满足条件；
- 匹配失败时自动执行自定义鼠标/键盘动作；
- 支持自定义动作脚本；
- 支持多套规则切换；
- 支持规则导入、导出；
- 支持 `F9` 读取鼠标位置；
- 支持 `F10` 开始/停止自动化；
- 支持简化动作脚本编辑；
- 支持动作在“简化脚本”和“JSON”之间转译；
- 支持试运行模式；
- 支持日志查看。

---

## 二、运行环境

建议使用 Windows 系统。

需要安装 Python 3。

安装依赖：

```bash
pip install pyautogui pyperclip keyboard
```

如果全局热键无法监听，可以尝试以管理员身份运行程序。

---

## 三、文件说明

程序运行后会生成或读取以下文件：

| 文件 | 作用 |
|---|---|
| `actions_config.json` | 保存动作、热键、复制快捷键等配置 |
| `rules_config.json` | 保存多套匹配规则 |

---

## 四、快速开始

### 1. 启动程序

运行主程序：

```bash
python 你的程序文件名.py
```

启动后会看到以下区域：

- 运行控制；
- 规则方案；
- 匹配规则；
- 动作配置；
- 日志。

---

### 2. 填写匹配规则

在“匹配规则”区域填写：

#### 蓝色词条

蓝色词条采用“至少匹配 X 条”的规则。

例如填写：

```text
火焰伤害
闪电伤害
冰霜伤害
```

如果设置：

```text
至少匹配 X 条蓝色词条 = 2
```

则剪贴板中至少包含其中 2 条，才算蓝色词条满足条件。

---

#### 红色词条

红色词条采用“必须全部包含”的规则。

例如填写：

```text
生命
抗性
```

则剪贴板中必须同时包含“生命”和“抗性”。

---

### 3. 选择动作

在“动作配置”区域选择要执行的动作，例如：

```text
改造
```

也可以新建、修改、保存自己的动作。

---

### 4. 测试匹配

可以先使用：

```text
复制并测试
```

或：

```text
测试当前剪贴板
```

确认规则是否能正确识别装备文本。

---

### 5. 开始自动化

点击：

```text
开始 / 停止 (F10)
```

或按：

```text
F10
```

开始自动运行。

再次按 `F10` 可停止。

---

## 五、热键说明

### 默认热键

| 热键 | 功能 |
|---|---|
| `F10` | 开始 / 停止自动化 |
| `F9` | 读取当前鼠标位置到动作配置区的 X/Y 输入框 |

---

### 修改热键

打开：

```text
actions_config.json
```

找到：

```json
"hotkey": "F10",
"mouse_position_hotkey": "F9"
```

可以修改为其他按键，例如：

```json
"hotkey": "F11",
"mouse_position_hotkey": "F8"
```

修改后需要重启程序。

注意：

- 修改热键后，全局热键会按配置文件生效；
- 但程序窗口按钮上的文字可能仍显示默认按键，例如“开始 / 停止 (F10)”；
- 实际生效热键以启动日志中的提示为准。

---

## 六、运行控制区域说明

运行控制区域包含以下按钮：

| 按钮/选项 | 说明 |
|---|---|
| `开始 / 停止 (F10)` | 开始或停止自动化流程 |
| `复制并测试` | 向游戏发送复制快捷键，并测试剪贴板内容 |
| `测试当前剪贴板` | 直接测试当前剪贴板内容 |
| `试运行(不执行点击)` | 只判断匹配结果，不执行鼠标点击动作 |

建议第一次使用某个规则或动作时，先勾选：

```text
试运行(不执行点击)
```

确认匹配逻辑正常后，再取消试运行。

---

## 七、规则方案说明

规则方案用于管理多套规则。

所有规则保存在：

```text
rules_config.json
```

---

### 1. 选择规则

在“选择规则”下拉框中选择已经保存的规则。

选择后，规则会自动载入到界面中。

---

### 2. 新建规则

点击：

```text
新建规则
```

程序会生成一个新规则名，例如：

```text
新规则_2
```

填写完成后点击：

```text
保存规则
```

---

### 3. 保存规则

点击：

```text
保存规则
```

会将当前界面中的规则保存到 `rules_config.json`。

如果修改了规则名：

- 选择“是”：重命名，删除旧规则；
- 选择“否”：另存为，保留旧规则。

---

### 4. 删除规则

选择要删除的规则，点击：

```text
删除规则
```

确认后即可删除。

---

### 5. 导入规则

点击：

```text
导入规则
```

选择 `.json` 规则文件。

支持导入：

- 单条规则文件；
- 多条规则文件。

如果导入规则和已有规则重名，程序会询问是否覆盖。

---

### 6. 导出当前规则

点击：

```text
导出当前规则
```

可以将当前规则保存为单独的 `.json` 文件，方便备份或分享。

---

### 7. 重载规则文件

如果你手动修改了：

```text
rules_config.json
```

点击：

```text
重载规则文件
```

可以重新读取文件内容。

---

## 八、匹配规则说明

### 蓝色词条规则

蓝色词条为“至少匹配”。

例如：

```text
火焰伤害
闪电伤害
冰霜伤害
```

设置：

```text
至少匹配 X 条蓝色词条 = 2
```

如果装备中有：

```text
火焰伤害
闪电伤害
```

则蓝色词条满足条件。

---

### 红色词条规则

红色词条为“必须全部包含”。

例如：

```text
生命
抗性
```

如果装备中缺少其中任意一条，则匹配失败。

---

### 最大尝试次数

用于限制自动化循环最多尝试多少次。

例如：

```text
最大尝试次数 = 200
```

当程序连续尝试 200 次仍未成功时，会自动停止。

---

## 九、动作配置说明

动作配置区域用于编辑自动执行的鼠标、键盘动作。

当前版本默认使用“简化动作脚本”显示，而不是复杂的 JSON。

例如：

```text
moveTo 110,270
click right
sleep 0.034
moveTo 330,440
click left
sleep 0.1
```

保存时，程序会自动将简化脚本转换成标准 JSON 存入：

```text
actions_config.json
```

---

## 十、简化动作脚本语法

### 1. 移动鼠标

格式：

```text
moveTo x,y
```

示例：

```text
moveTo 110,270
```

如果需要鼠标移动耗时，可以加第三个参数：

```text
moveTo x,y,duration
```

示例：

```text
moveTo 110,270,0.02
```

说明：

- `x`：鼠标横坐标；
- `y`：鼠标纵坐标；
- `duration`：鼠标移动耗时，单位秒。

一般可以省略第三个参数。

---

### 2. 点击鼠标

格式：

```text
click left
click right
click middle
```

示例：

```text
click right
```

也可以指定点击次数：

```text
click left 2
```

表示左键点击 2 次。

还可以指定点击间隔：

```text
click left 2 0.05
```

表示左键点击 2 次，每次间隔 0.05 秒。

---

### 3. 等待

格式：

```text
sleep 秒数
```

示例：

```text
sleep 0.1
```

表示等待 0.1 秒。

---

### 4. 组合键

格式：

```text
hotkey 按键组合
```

示例：

```text
hotkey ctrl+shift+c
```

---

### 5. 按下并释放单个按键

格式：

```text
press 按键
```

示例：

```text
press f1
```

---

### 6. 按下按键不释放

格式：

```text
keyDown 按键
```

示例：

```text
keyDown shift
```

---

### 7. 释放按键

格式：

```text
keyUp 按键
```

示例：

```text
keyUp shift
```

---

### 8. 注释

可以使用：

```text
#
```

或：

```text
//
```

作为注释。

示例：

```text
# 右键改造石
moveTo 110,270
click right

// 左键装备
moveTo 330,440
click left
```

注释不会参与保存和执行。

---

## 十一、鼠标取点功能

程序支持按 `F9` 读取当前鼠标位置。

### 使用方法

1. 将鼠标移动到需要点击的位置；
2. 按：

```text
F9
```

3. 程序会将坐标写入：

```text
X
Y
```

输入框。

日志示例：

```text
已读取鼠标位置: (110, 270)
```

---

## 十二、动作插入按钮说明

### 鼠标取点区域

| 按钮 | 功能 |
|---|---|
| `读取鼠标位置 (F9)` | 读取当前鼠标坐标到 X/Y 输入框 |
| `插入 moveTo` | 插入 `moveTo x,y` |
| `插入 moveTo + 右键` | 插入移动、右键点击、等待 |
| `插入 moveTo + 左键` | 插入移动、左键点击、等待 |

示例：

点击：

```text
插入 moveTo + 右键
```

会插入：

```text
moveTo 110,270
click right
sleep 0.05
```

点击：

```text
插入 moveTo + 左键
```

会插入：

```text
moveTo 110,270
click left
sleep 0.05
```

如果不需要其中的：

```text
sleep 0.05
```

可以手动删除或修改。

---

### 插入点击区域

| 按钮 | 功能 |
|---|---|
| `click left` | 插入左键点击 |
| `click right` | 插入右键点击 |
| `click middle` | 插入中键点击 |

---

### 插入 sleep 区域

先在输入框填写等待时间，例如：

```text
0.034
```

然后点击：

```text
插入 sleep
```

会插入：

```text
sleep 0.034
```

---

### 插入按键区域

在“按键”输入框填写按键，然后点击对应按钮。

| 按钮 | 示例输入 | 插入结果 |
|---|---|---|
| `插入 hotkey` | `ctrl+shift+c` | `hotkey ctrl+shift+c` |
| `插入 press` | `f1` | `press f1` |
| `插入 keyDown` | `shift` | `keyDown shift` |
| `插入 keyUp` | `shift` | `keyUp shift` |

---

## 十三、动作转译按钮说明

动作配置区域提供以下按钮：

| 按钮 | 功能 |
|---|---|
| `插入模板` | 插入一个简化动作模板 |
| `转译为简化` | 将当前动作内容转换为简化脚本 |
| `转译为 JSON` | 将当前动作内容转换为 JSON |
| `清空动作` | 清空当前动作编辑框 |

---

### 示例：JSON 转简化

当前内容：

```json
[
  {
    "type": "moveTo",
    "x": 110,
    "y": 270
  },
  {
    "type": "click",
    "button": "right"
  },
  {
    "type": "sleep",
    "time": 0.034
  }
]
```

点击：

```text
转译为简化
```

后变成：

```text
moveTo 110,270
click right
sleep 0.034
```

---

### 示例：简化转 JSON

当前内容：

```text
moveTo 110,270
click right
sleep 0.034
```

点击：

```text
转译为 JSON
```

后变成：

```json
[
  {
    "type": "moveTo",
    "x": 110,
    "y": 270
  },
  {
    "type": "click",
    "button": "right"
  },
  {
    "type": "sleep",
    "time": 0.034
  }
]
```

---

## 十四、制作“改造”动作示例

目标动作：

```text
moveTo 110,270
click right
sleep 0.034
moveTo 330,440
click left
sleep 0.1
```

操作步骤：

1. 在动作配置中选择或新建动作，例如：

```text
改造
```

2. 将鼠标移动到“改造石”位置；
3. 按：

```text
F9
```

4. 点击：

```text
插入 moveTo + 右键
```

5. 将生成的：

```text
sleep 0.05
```

手动改成：

```text
sleep 0.034
```

6. 将鼠标移动到装备位置；
7. 按：

```text
F9
```

8. 点击：

```text
插入 moveTo + 左键
```

9. 将生成的：

```text
sleep 0.05
```

手动改成：

```text
sleep 0.1
```

10. 点击：

```text
保存
```

最终动作示例：

```text
moveTo 110,270
click right
sleep 0.034
moveTo 330,440
click left
sleep 0.1
```

---

## 十五、制作“重铸点金”动作示例

目标流程：

1. 右键重铸石；
2. 左键装备；
3. 右键点金石；
4. 左键装备。

简化动作示例：

```text
moveTo 490,270
sleep 0.03
click right
sleep 0.05
moveTo 329,448
sleep 0.03
click left
sleep 0.05
moveTo 430,400
sleep 0.03
click right
sleep 0.05
moveTo 329,448
sleep 0.03
click left
sleep 0.18
```

坐标请根据自己的游戏界面重新取点。

---

## 十六、试运行模式

勾选：

```text
试运行(不执行点击)
```

后，程序只会：

- 发送复制快捷键；
- 读取剪贴板；
- 判断词条；
- 输出日志；
- 提示匹配成功或失败；

但不会执行鼠标点击、键盘动作。

适用场景：

- 调试匹配规则；
- 检查剪贴板是否能正常读取；
- 确认复制快捷键是否有效；
- 避免误点游戏内物品。

---

## 十七、日志说明

日志区域会显示：

- 热键注册状态；
- 当前加载规则；
- 动作保存状态；
- 每次读取剪贴板的结果；
- 蓝色词条命中情况；
- 红色词条缺失情况；
- 动作执行状态；
- 错误信息。

如果程序异常，优先查看日志。

---

## 十八、配置文件说明

### actions_config.json

示例：

```json
{
  "hotkey": "F10",
  "mouse_position_hotkey": "F9",
  "copy_hotkey": [
    "ctrl",
    "shift",
    "c"
  ],
  "actions": {
    "改造": [
      {
        "type": "moveTo",
        "x": 110,
        "y": 270
      },
      {
        "type": "click",
        "button": "right"
      },
      {
        "type": "sleep",
        "time": 0.034
      },
      {
        "type": "moveTo",
        "x": 330,
        "y": 440
      },
      {
        "type": "click",
        "button": "left"
      },
      {
        "type": "sleep",
        "time": 0.1
      }
    ]
  }
}
```

字段说明：

| 字段 | 说明 |
|---|---|
| `hotkey` | 开始/停止自动化的全局热键 |
| `mouse_position_hotkey` | 读取鼠标位置的全局热键 |
| `copy_hotkey` | 游戏内复制装备文本的快捷键 |
| `actions` | 所有动作配置 |

---

### rules_config.json

示例：

```json
{
  "__selected_rule__": "默认规则",
  "默认规则": {
    "name": "默认规则",
    "blue_rules": [
      "火焰伤害",
      "闪电伤害"
    ],
    "red_rules": [
      "生命"
    ],
    "min_blue_match": 2,
    "max_attempts": 200,
    "action_name": "改造",
    "dry_run": false,
    "copy_hotkey": [
      "ctrl",
      "shift",
      "c"
    ]
  }
}
```

字段说明：

| 字段 | 说明 |
|---|---|
| `__selected_rule__` | 当前选中的规则名 |
| `name` | 规则名 |
| `blue_rules` | 蓝色词条列表 |
| `red_rules` | 红色词条列表 |
| `min_blue_match` | 至少匹配多少条蓝色词条 |
| `max_attempts` | 最大尝试次数 |
| `action_name` | 匹配失败后执行的动作 |
| `dry_run` | 是否试运行 |
| `copy_hotkey` | 复制装备文本快捷键 |

---

## 十九、常见问题

---

### 1. 按 `F10` 没有反应？

可能原因：

- 没有安装 `keyboard`；
- 程序没有管理员权限；
- 热键被其他软件占用；
- 游戏屏蔽了全局热键。

解决方法：

1. 安装：

```bash
pip install keyboard
```

2. 以管理员身份运行程序；
3. 修改 `actions_config.json` 中的：

```json
"hotkey": "F10"
```

为其他按键；
4. 修改后重启程序。

---

### 2. 按 `F9` 没有反应？

可能原因：

- 没有安装 `keyboard`；
- `F9` 被游戏占用；
- 程序没有管理员权限。

解决方法：

修改：

```json
"mouse_position_hotkey": "F9"
```

为其他按键，例如：

```json
"mouse_position_hotkey": "F8"
```

然后重启程序。

---

### 3. 修改热键后窗口按钮文字没有变化？

这是正常现象。

程序窗口按钮文字可能仍显示：

```text
开始 / 停止 (F10)
```

但实际生效的热键以配置文件和启动日志为准。

例如日志显示：

```text
全局开始/停止热键已注册: F11
```

则实际开始/停止热键是 `F11`。

---

### 4. 剪贴板读取失败？

请检查：

- 游戏窗口是否有焦点；
- 游戏内复制快捷键是否正确；
- 程序配置的 `copy_hotkey` 是否和游戏一致；
- 是否安装了 `pyperclip`；
- 游戏是否允许剪贴板读取。

默认复制快捷键为：

```text
Ctrl + Shift + C
```

如果游戏内不是这个键，请修改：

```json
"copy_hotkey": [
  "ctrl",
  "shift",
  "c"
]
```

---

### 5. 匹配总是失败？

请检查：

- 蓝色词条是否填写正确；
- 红色词条是否填写过多；
- “至少匹配 X 条蓝色词条”是否设置过高；
- 装备文本中是否真的包含这些词条；
- 是否使用了装备复制快捷键；
- 剪贴板是否成功读取。

建议使用：

```text
复制并测试
```

或：

```text
测试当前剪贴板
```

查看匹配结果。

---

### 6. 动作点击位置不准？

可能原因：

- 游戏窗口位置发生变化；
- 游戏分辨率发生变化；
- 游戏窗口模式/全屏模式发生变化；
- 取点时鼠标位置不准确；
- 游戏界面缩放发生变化。

解决方法：

1. 固定游戏分辨率；
2. 固定游戏窗口模式；
3. 不要移动游戏窗口；
4. 重新按 `F9` 取点；
5. 保存动作后再次测试；
6. 先使用试运行确认逻辑。

---

### 7. 动作编辑框提示解析失败？

请检查简化脚本格式是否正确。

正确示例：

```text
moveTo 110,270
click right
sleep 0.034
```

错误示例：

```text
moveTo 110
click
sleep
```

常见错误：

- 缺少坐标；
- 缺少点击类型；
- 缺少等待时间；
- 使用中文标点；
- 多个参数之间格式错误。

建议尽量使用程序提供的插入按钮生成内容。

---

### 8. 动作保存失败？

请检查：

- 动作名是否为空；
- 动作脚本是否有语法错误；
- `actions_config.json` 是否被其他程序占用；
- 程序目录是否有写入权限。

---

### 9. 程序会不会误操作？

会。

因为程序可以自动控制鼠标和键盘，所以配置错误或坐标错误时，可能会点击到非预期位置。

建议：

- 初次使用先勾选试运行；
- 调试动作前关闭敏感窗口；
- 不要在重要操作时使用未测试的动作；
- 确保随时能按 `F10` 停止。

---

## 二十、安全提示

- 本工具涉及自动鼠标、键盘操作和剪贴板读取；
- 使用前请确认相关游戏规则和用户协议；
- 请勿用于违反游戏规则的场景；
- 因使用本工具产生的后果由使用者自行承担。
```